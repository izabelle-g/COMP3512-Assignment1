This is just a text file showing what the table(database) names are and their columns for easy reference.

artists:
artist_id,
artist_name,
artist_type_id

genres:
genre_id,
genre_name

types:
type_id,
type_name

songs:
song_id,
title,
artist_id,
genre_id,
year,
bpm,
energy,
danceability,
loudness,
liveness,
valence,
duration,
acousticness,
speechiness,
popularity